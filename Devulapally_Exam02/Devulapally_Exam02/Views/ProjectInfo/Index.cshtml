@model IEnumerable<Devulapally_Exam02.Models.ProjectInfo>

@{
    ViewBag.Title = "ProjectInfo";
}

<h2>ProjectInfo</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Employees.LastName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Projects.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AssignmentDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Employees.LastName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Projects.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AssignmentDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProjectInfoID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProjectInfoID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProjectInfoID })
        </td>
    </tr>
}

</table>
